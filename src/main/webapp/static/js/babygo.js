window.___gcfg = {
	lang : "zh-CN",
	parsetags : "onload"
};
$(document).ready(function() {
	//demoContent._init()
	//path == "main" ? mainContent._init() : (path == "demo" || path == "index")? demoContent._init() : path == "api" ? apiContent._init() : path == "faq" ? faqContent._init() : path == "donate" && donateContent._init();
	
	if(path == ""){
		path = "index";
	}
	
});
